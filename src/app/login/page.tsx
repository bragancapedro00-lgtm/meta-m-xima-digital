import LoginView from '@/components/auth/LoginView';

export const metadata = {
  title: 'Acesso de Colaboradores | Meta Máxima Digital',
  description: 'Portal de login e acesso para colaboradores e membros da equipe da agência.',
};

export default function LoginPage() {
  return <LoginView />;
}
