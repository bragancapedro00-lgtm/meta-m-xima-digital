import SettingsView from '@/components/settings/SettingsView';

export const metadata = {
  title: 'Configurações | Meta Máxima Digital',
  description: 'Configurações do sistema, equipe e integrações.',
};

export default function SettingsPage() {
  return <SettingsView />;
}
